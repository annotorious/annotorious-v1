goog.provide('yuma.model.geom.Point');

/**
 * A point geometry primitive.
 * @param {number} x the x coordinate
 * @param {number} y the y coordinate
 * @constructor
 */
yuma.model.geom.Point = function(x, y) {
  this.x = x;
  this.y = y;
}

